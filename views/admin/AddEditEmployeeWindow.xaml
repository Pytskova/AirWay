<Window x:Class="AirlineApp.Views.AddEditEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудник" Height="350" Width="300" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameBox" Grid.Row="0" Grid.Column="1" Margin="5,2"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="5,2"/>

        <TextBlock Text="ФИО:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="FullNameBox" Grid.Row="2" Grid.Column="1" Margin="5,2"/>

        <TextBlock Text="Роль:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="RoleBox" Grid.Row="3" Grid.Column="1" Margin="5,2">
            <ComboBoxItem Content="Admin"/>
            <ComboBoxItem Content="Pilot"/>
            <ComboBoxItem Content="CabinCrew"/>
            <ComboBoxItem Content="Technician"/>
        </ComboBox>

        <TextBlock Text="Налёт часов:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="FlightHoursBox" Grid.Row="4" Grid.Column="1" Margin="5,2"/>

        <TextBlock Text="Срок медсправки:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="MedCertPicker" Grid.Row="5" Grid.Column="1" Margin="5,2"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Сохранить" Width="80" Margin="5" Click="Save_Click"/>
            <Button Content="Отмена"   Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>