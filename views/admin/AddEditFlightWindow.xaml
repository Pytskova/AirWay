<Window x:Class="AirlineApp.Views.Admin.AddEditFlightWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Height="360" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Номер рейса -->
            <RowDefinition Height="Auto"/>
            <!-- Маршрут -->
            <RowDefinition Height="Auto"/>
            <!-- Рег. номер -->
            <RowDefinition Height="Auto"/>
            <!-- План. вылет -->
            <RowDefinition Height="Auto"/>
            <!-- План. прилёт -->
            <RowDefinition Height="Auto"/>
            <!-- Статус -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <!-- Кнопки -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Номер рейса -->
        <TextBlock Text="Номер рейса:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox    x:Name="TbFlightNumber" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Маршрут -->
        <TextBlock Text="Маршрут:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox    x:Name="TbRoute" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Рег. номер самолёта -->
        <TextBlock Text="Рег. номер:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox  x:Name="CbAircraftRegistration"
                   Grid.Row="2" Grid.Column="1" Margin="5"
                   ItemsSource="{Binding Registrations}"
                   SelectedItem="{Binding EditingFlight.AircraftRegistration, Mode=TwoWay}" />

        <!-- Плановый вылет -->
        <TextBlock Text="План. вылет:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DpScheduledDeparture" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <!-- Плановый прилёт -->
        <TextBlock Text="План. прилёт:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DpScheduledArrival" Grid.Row="4" Grid.Column="1" Margin="5"/>

        <!-- Статус рейса -->
        <TextBlock Text="Статус:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CbStatus"
                  Grid.Row="5" Grid.Column="1" Margin="5"
                  ItemsSource="{Binding StatusesDisplay}"
                  DisplayMemberPath="Value"
                  SelectedValuePath="Key"
                  SelectedValue="{Binding SelectedStatus, Mode=TwoWay}" />

        <!-- Кнопки Сохранить/Отмена -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Отмена"    Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
