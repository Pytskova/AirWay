<Window x:Class="AirlineApp.Views.AddEditSparePartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Запчасть" Height="200" Width="300" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Название:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,2"/>
        <TextBox x:Name="PartNameBox" Grid.Row="0" Grid.Column="1" Margin="5,2"/>

        <TextBlock Text="Количество:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,2"/>
        <TextBox x:Name="QuantityBox" Grid.Row="1" Grid.Column="1" Margin="5,2"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Margin="5" Click="Save_Click"/>
            <Button Content="Отмена"   Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>