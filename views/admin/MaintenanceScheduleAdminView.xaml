<UserControl x:Class="AirlineApp.Views.Admin.MaintenanceScheduleAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 8">
            <Button Content="Добавить" Width="100" Margin="4" Click="BtnAdd_Click"/>
            <Button Content="Изменить" Width="100" Margin="4" Click="BtnEdit_Click"/>
            <Button Content="Удалить" Width="100" Margin="4" Click="BtnDelete_Click"/>
        </StackPanel>
        <DataGrid x:Name="ScheduleGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Бортовой номер" Binding="{Binding AircraftRegistration}" Width="120"/>
                <DataGridTextColumn Header="Дата ТО" Binding="{Binding MaintenanceDate}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
