<UserControl x:Class="AirlineApp.Views.Crew.CrewTrainingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="10">
        <!-- Кнопка обновить -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
            <Button Content="Обновить"
                    Click="Refresh_Click"
                    Width="80"/>
        </StackPanel>

        <!-- Заголовок -->
        <TextBlock Text="Мои обучения"
                   FontSize="16"
                   FontWeight="Bold"
                   DockPanel.Dock="Top"
                   Margin="0,0,0,10"/>

        <!-- Таблица назначений -->
        <DataGrid x:Name="TrainingGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тема"
                                    Binding="{Binding TrainingEvent.Topic}"
                                    Width="*"/>
                <DataGridTextColumn Header="Дата"
                                    Binding="{Binding TrainingEvent.EventDate, StringFormat=dd.MM.yyyy}"
                                    Width="120"/>
                <DataGridCheckBoxColumn Header="Пройдено"
                                        Binding="{Binding Passed}"
                                        Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
